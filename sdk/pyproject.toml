[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "agentcourt"
version = "0.3.0"
description = "Python SDK for AgentCourt Autonomous Escrow and Arbitration on Base"
readme = "README.md"
authors = [{ name = "AgentCourt Team" }]
dependencies = [
    "web3>=6.0.0"
]
